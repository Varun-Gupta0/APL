// Re-export from data.ts for convenience
export * from './data';

// XP level thresholds (also used by progression service)
export const XP_PER_LEVEL = [0, 100, 250, 500, 900, 1500, 2300, 3300, 4600, 6200, 10000];
